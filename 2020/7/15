今天将昨天找的Defense-gan: Protecting classifiers against adversarial attacks using generative models的论文看完（ICLR2018）
这个论文的创新点就是用GAN来防御对抗攻击，先用正常的样本训练一个GAN，借助GAN的数据表征能力，
在预测之前，先将异常样本提交给生成器，将数据投影到正常的分布范围内，然后用投影后的数据来预测。
这个论文跟我昨天组会想的用gAN生成子模型替代真实模型有点类似，都是进行数据表征，
只不过这个论文是表示数据样本，而我要表征模型，感觉有点相通，应该可以借鉴。这个论文也给了代码，准备跑跑看。
